interface:
  display_name: "中文去 AI 味写作 Skill"
  short_description: "写作、编辑和审查中文，减少常见 AI 味"
  default_prompt: "Use $anti-ai-writing-kit-zh. Open SKILL.md at the exact registered path, read through EOF, load the active rules, and run the final gate before returning Chinese prose."
